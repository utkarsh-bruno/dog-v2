info:
  name: echo
  type: http
  seq: 4

http:
  method: GET
  url: httpS
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
