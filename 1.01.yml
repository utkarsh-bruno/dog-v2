info:
  name: "1.01"
  type: http
  seq: 6

http:
  method: GET
  url: https://localhost:3000/
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
